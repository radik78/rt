<header>

    
    <div><%= print_status_sign if Rails.env.development? %></div>
    
    <%  links = link_hash  %>
    <%  links.each do |link| %>
        <nav class="round">
            <ul><li><%= link_to link[:text], link[:path] %></li></ul>
        </nav>  
    <%  end                 %>
    <div><h2><%= @title %> </h2></div>

</header>