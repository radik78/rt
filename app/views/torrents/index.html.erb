

<% @torrents.each do |trnt| 	%>
	<div class="container">
		<div class="round messages" >
			<p class="small_bottom">
					<%
					 #debugger
					  %>
					<% user = trnt.user rescue User.find(trnt.user_id) %>
					<div class = "time_mess ">
						<%= "#{user.name} разместил #{trnt.created_at.strftime("%Y-%m-%d %H:%M")}: #{trnt.description}" %>
					</div>
					<div class = "text_mess ">
						<%= 	 %>
						<%= link_to trnt.filename, torrent_path(trnt.id) %>
					</div>				
			</p>
		</div>		
	</div>

<% end	%>
